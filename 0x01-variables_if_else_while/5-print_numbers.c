#include <stdio.h>

/**
 * main - Program entry point
 *
 * Return: 0 on success. Error code otherwizse
 */

int main(void)
{
	int i;
	
	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
