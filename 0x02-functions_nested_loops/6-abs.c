#include "main.h"
#include <stdlib.h>
/**
 *_abs - computes the absolute value of an integer
 * @n: the specified integer
 *
 * Return: returns the absolute value of an integer
 */
int _abs(int n)
{
	int absNum = abs(n);
return (absNum);
}
